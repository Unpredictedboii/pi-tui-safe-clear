# pi-tui-safe-clear

Clears terminal scrollback/history when `pi-cli` starts and on `/quit`, **while preserving** the pi TUI header, skills list, extensions, and status bar.

## 📦 Install

```bash
npm install pi-tui-safe-clear
```
